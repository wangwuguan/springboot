mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.demo.pojo
# 数据库连接池
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/sprintboot?useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true
    driver-class-name: com.mysql.cj.jdbc.Driver
